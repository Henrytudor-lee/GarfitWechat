# fitness_wechat 项目上下文

## 基本信息
- **项目类型**: 微信小程序 + 云开发（腾讯云 CloudBase）+ PostgreSQL
- **路径**: /Volumes/world/program/fitness_wechat
- **运行**: 微信开发者工具打开此目录
- **分支**: master，HEAD @ a462fa8，已与 origin 同步

## 项目结构
```
cloudfunctions/     # 8个云函数
  exercise/         # 动作记录 + favor/practiced（add/list/update/delete/getMaxWeight/toggleFavorite/markPracticed/getUserExercises）
  exerciseLibrary/  # 动作库查询（list/detail/favorites/toggleFavorite/count）
  session/          # 训练会话（create/getRunning/finish/list/getById/delete）
  loginByWx/        # 微信登录（返回 favor_exercises + practiced_exercises）
  profile/          # 用户资料
  stats/            # 统计数据

miniprogram/pages/  # 6个页面
  index/            # 当前训练页（首页，含计时器）
  workout/          # 训练记录页
  library/          # 动作库浏览（favor/practiced 标记）
  exercise-detail/  # 动作详情
  history/          # 历史记录
  stats/            # 统计页

miniprogram/components/  # 6个组件
  edit-exercise-modal/   # 编辑动作弹窗
  add-exercise-modal/    # 添加动作弹窗（favor/practiced 标记 + heart icon）
  session-detail-modal/  # 训练详情弹窗
  workout-summary-modal/ # 训练总结弹窗
  appbar/            # 顶部导航栏
  custom-tabbar/     # 自定义tabbar
```

## 数据库核心表

### exercises（训练动作记录）
| 字段 | 说明 |
|------|------|
| id | 自增主键 |
| session_id | 关联 sessions.id |
| exercise_id | 关联 exercises_library.id |
| name_zh | 中文名称（NOT NULL） |
| name_en | 英文名称 |
| image_name | 图片文件名 |
| video_name | 视频文件名 |
| weight | 重量数值 |
| weight_unit | 单位（默认 'kg'，统一 camelCase） |
| reps | 次数 |
| _openid | 微信 openid（数据隔离） |

### sessions（训练会话）
- id, user_id, start_time, end_time, duration, status, is_done

### exercises_library（动作库）
- **注意**：主键是 `id`（不是 `_id`），前端所有读取/写入必须用 `id`
- id, name, name_zh, image_name, video_name, equipment_id, body_part_id, is_favorite
- body_part_id 可以是逗号分隔字符串如 `'19,1'`，表示多部位

### users（用户表）
- id, _openid, favor_exercises, practiced_exercises, role, status
- **favor_exercises / practiced_exercises 字段类型**: VARCHAR，存储逗号分隔字符串如 `'1,2,12,99,22'`
- **不是 JSON 数组**，读取时用 `split(',').filter(Boolean).map(Number)`，写入时用 `join(',')`

## 关键约定

1. **weight_unit 统一用 camelCase** — 不得用 `unit` 或 `weightUnit`
2. **_openid 数据隔离** — 前端传 openid 到云函数，后端 WHERE 条件必须含 `_openid`
3. **exercises_library 表用 `id` 不是 `_id`** — 所有前端读取/写入统一用 `item.id`
4. **image 前缀**: `cloud://cloudbase-d9gwy4qvodf85fe69.636c-cloudbase-d9gwy4qvodf85fe69-1427916036/media/images`
5. **video 前缀**: `cloud://cloudbase-d9gwy4qvodf85fe69.636c-cloudbase-d9gwy4qvodf85fe69-1427916036/media/videos`
6. **云函数调用规范**：
   - exercise/add: 必须字段 session_id, exercise_id, name_zh, weight_unit
   - exercise/list: 返回 exercises 表所有字段
   - exercise/update: weight, reps, weight_unit 三字段同时更新
   - exercise/delete: 需要 id + session_id + openid
   - exercise/toggleFavorite: exercise_id + openid，VARCHAR 逗号格式
   - exercise/markPracticed: exercise_id + openid，fire-and-forget 异步
   - exercise/getUserExercises: openid，返回 favor_exercises + practiced_exercises 数组
   - loginByWx: 返回 userId, openid, favor_exercises[], practiced_exercises[]

## Favor / Practiced 功能

### 数据存储格式
- VARCHAR 逗号分隔字符串：`'1,2,12,99,22'`
- 读取：`raw.split(',').filter(Boolean).map(Number)` → Number[]
- 写入：`updated.join(',')`

### 云函数 action
- **toggleFavorite**: 读取 → 增/删 → 写回 users 表 favor_exercises
- **markPracticed**: 读取 → 不存在则 push → 写回 users 表 practiced_exercises；fire-and-forget，不 await
- **getUserExercises**: 返回 `{ favor_exercises: Number[], practiced_exercises: Number[] }`
- **loginByWx**: 登录时一并返回 favor_exercises 和 practiced_exercises

### 前端存储
- `app.globalData.favorExercises: Number[]` — 来自 loginByWx
- `app.globalData.practicedExercises: Number[]` — 来自 loginByWx
- storage 同步存储：`wx.setStorageSync('favorExercises', [])`

### UI 标记
- `.done-badge`（绿色）：练习过的动作，左上角 absolute 定位
- `.fav-icon-wrap`（心形图标）：左下角 absolute，点击 toggle favorite
- 列表排序：practiced > favorited > rest of list

### WeChat dataset 注意事项
- WXML `data-weight-unit="kg"` → JS `dataset.weightUnit`（camelCase，不是 underscore）
- WXML `data-id="{{item.id}}"` → JS `dataset.id`

## UI 样式参考
**参考项目**: garcia-fitness-new（/Volumes/world/program/garcia-fitness-new）

运动卡片布局：
- header：左侧图片(120rpx圆角) + 名称 + "X sets" + 右侧总重量
- sets：2列 grid，gap-2（16rpx），padding p-3（24rpx）
- 每格：绿色圆圈序号 + 白色数字 + 白色 × + 白色数字 + 白色单位

**关键色值**：
- accent: #ccf200
- 背景: #131313
- 序号圆圈: 底 #ccf200，数字 #131313
- × 符号: 白色 #fff
- 单位（kg/reps）: 白色 #fff
- 次要文字: #71717a

## 已完成
- 运动卡片 2 列 grid 样式
- 总重量标签改为 "TOTAL VOLUME"
- weight_unit 字段统一命名
- edit-exercise-modal 删除逻辑修复
- muscle/equipment 英文名显示
- total volume badge
- ADD TO WORKOUT 按钮换行问题（`white-space: nowrap` + `height: 96rpx`）
- 弹窗 header 改为 "SET WEIGHT & REPS"（重量输入 label 保持 "WEIGHT (kg/lb)"）
- video 无内容时显示提示文字
- rest timer 在添加/编辑动作后重置
- Equipment chip 名称始终显示（移除了 `display: none`）
- favor/practiced 完整功能（云函数 + UI 标记 + 列表排序）
- loginByWx 返回 favor + practiced 列表
- 所有 `._id` 改为 `id`（exercises_library 表主键）
- 全局 `wx:key` 统一为 `id`

## 待处理
- exercise-detail 页 `addToWorkout` 方法需添加 `markPracticed` 调用（fire-and-forget）
- cloudfunctions/exercise 需重新部署到腾讯云（新增的 3 个 action）