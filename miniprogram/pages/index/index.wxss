/* pages/index/index.wxss — garcia-fitness-new dark theme */
.page {
  min-height: 100vh;
  background: #131313;
  padding: 150rpx 24rpx 200rpx;
}

/* Timer Hero Card */
.timer-card {
  background: rgba(0, 0, 0, 0.30);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 32rpx;
  padding: 48rpx 32rpx 36rpx;
  text-align: center;
  margin: 24rpx 0;
  position: relative;
  overflow: hidden;
}

.timer-label {
  font-size: 20rpx;
  font-weight: 800;
  color: rgba(204, 242, 0, 0.7);
  letter-spacing: 0.3em;
  text-transform: uppercase;
  margin-bottom: 8rpx;
}

.timer-display {
  font-family: 'Lexend Deca', 'Lexend', sans-serif;
  font-size: 96rpx;
  font-weight: 900;
  color: #ffffff;
  letter-spacing: 0.05em;
  line-height: 1;
  text-shadow: 0 0 30rpx rgba(204, 242, 0, 0.3);
}

.timer-stats-row {
  display: flex;
  justify-content: space-around;
  align-items: flex-start;
  margin-top: 32rpx;
}

.timer-stat {
  text-align: center;
}

.stat-val {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 40rpx;
  font-weight: 900;
  color: #fff;
  line-height: 1;
}

.stat-val.text-primary {
  color: var(--primary-text);
}

.stat-key {
  font-size: 18rpx;
  font-weight: 800;
  color: rgba(255, 255, 255, 0.35);
  letter-spacing: 0.15em;
  text-transform: uppercase;
  margin-top: 6rpx;
}

.stat-divider {
  width: 1rpx;
  height: 60rpx;
  background: rgba(255, 255, 255, 0.08);
  align-self: center;
}

/* Start/Stop Button */
.start-btn {
  width: 100%;
  border-radius: 100rpx;
  font-family: 'Lexend Deca', sans-serif;
  font-size: 32rpx;
  font-weight: 900;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  padding: 28rpx 0;
  margin: 24rpx 0;
  border: none;
  line-height: 1;
}
.start-btn::after { border: none; }
.start-btn.start {
  background: var(--primary);
  color: #000;
  box-shadow: 0 0 40rpx rgba(204, 242, 0, 0.35);
}
.start-btn.stop {
  background: #ef4444;
  color: #fff;
  box-shadow: 0 0 30rpx rgba(239, 68, 68, 0.3);
}

/* FAB */
.fab {
  position: fixed;
  bottom: 200rpx;
  left: 32rpx;
  width: 112rpx;
  height: 112rpx;
  background: var(--primary);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 0 30rpx rgba(204, 242, 0, 0.5);
  z-index: 100;
}
.fab-icon {
  font-size: 64rpx;
  color: #000;
  font-weight: 300;
  line-height: 1;
  margin-top: -6rpx;
}

/* Section */
.section {
  margin-top: 32rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.section-title {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 26rpx;
  font-weight: 900;
  color: #fff;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.section-badge {
  font-size: 18rpx;
  font-weight: 800;
  color: #52525b;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}
.section-badge.active {
  color: var(--primary-text);
}

.section-link {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 22rpx;
  font-weight: 700;
  color: var(--primary-text);
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

/* Exercise Card */
.exercise-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.exercise-card {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 20rpx;
  overflow: hidden;
}

.ec-header {
  display: flex;
  align-items: center;
  padding: 16rpx;
  gap: 12rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.05);
}

.ec-img-wrap {
  width: 120rpx;
  height: 120rpx;
  border-radius: 16rpx;
  overflow: hidden;
  flex-shrink: 0;
  background: rgba(255, 255, 255, 0.03);
}

.ec-img {
  width: 100%;
  height: 100%;
}

.ec-img-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48rpx;
}

.ec-info {
  flex: 1;
  min-width: 0;
}

.ec-name {
  font-size: 28rpx;
  font-weight: 700;
  color: #fff;
  margin-bottom: 4rpx;
  overflow: hidden;
}

.ec-sets-count {
  font-size: 24rpx;
  color: #71717a;
  font-weight: 500;
}

.ec-volume {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
}

.ec-vol-num {
  font-size: 24rpx;
  font-weight: 700;
  color: var(--primary-text);
}

.ec-vol-label {
  font-size: 20rpx;
  color: #71717a;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.ec-sets-grid {
  padding: 24rpx;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.ec-set-item {
  display: flex;
  align-items: center;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  padding: 16rpx 24rpx;
  column-gap: 16rpx;
}

.ec-set-num {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: var(--primary);
  color: #131313;
  font-size: 20rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.ec-set-val {
  font-size: 26rpx;
  font-weight: 500;
  color: #fff;
  flex: 1;
  overflow: hidden;
}

.ec-set-val.reps {
  flex: .7;
  text-align: end;
}

.ec-set-unit {
  font-size: 20rpx;
  color: #fff;
  font-weight: 400;
}

.ec-set-sep {
  font-size: 22rpx;
  color: #fff;
  flex-shrink: 0;
}

/* Empty Card */
.empty-card {
  background: rgba(255, 255, 255, 0.03);
  border: 1px dashed rgba(255, 255, 255, 0.08);
  border-radius: 20rpx;
  padding: 60rpx 40rpx;
  text-align: center;
}
.empty-hint {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 22rpx;
  font-weight: 700;
  color: #3f3f46;
  letter-spacing: 0.15em;
  text-transform: uppercase;
}

/* Recent Sessions */
.recent-list {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.recent-item {
  background: rgba(255, 255, 255, 0.03);
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.recent-date {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 24rpx;
  font-weight: 700;
  color: #fff;
}

.recent-info {
  display: flex;
  gap: 16rpx;
  align-items: center;
}

.recent-stat {
  font-size: 20rpx;
  font-weight: 700;
  color: #71717a;
  letter-spacing: 0.05em;
}

/* ====== History Bento Grid ====== */
.history-session-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

.history-session-item {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 20rpx;
  padding: 20rpx 24rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.history-session-time {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 24rpx;
  font-weight: 700;
  color: var(--primary-text);
}

.history-session-info {
  display: flex;
  gap: 16rpx;
  align-items: center;
}

.history-session-stat {
  font-size: 20rpx;
  font-weight: 700;
  color: #71717a;
  letter-spacing: 0.05em;
}

.bento-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12rpx;
}

.bento-card {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 24rpx;
  padding: 24rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 200rpx;
  position: relative;
  overflow: hidden;
}

.bento-label {
  font-size: 18rpx;
  font-weight: 800;
  color: rgba(255, 255, 255, 0.35);
  letter-spacing: 0.15em;
  text-transform: uppercase;
}

.bento-day {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 72rpx;
  font-weight: 900;
  color: #fff;
  line-height: 1;
  margin: 8rpx 0 4rpx;
}

.bento-month {
  font-size: 20rpx;
  font-weight: 800;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.bento-month.text-primary { color: var(--primary-text); }
.bento-month.text-muted { color: rgba(255, 255, 255, 0.25); }

.bento-progress-bar {
  width: 100%;
  height: 6rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 100rpx;
  overflow: hidden;
  margin-top: 16rpx;
}

.bento-progress-fill {
  height: 100%;
  background: var(--primary);
  border-radius: 100rpx;
}

.bento-progress-fill.muted {
  background: rgba(255, 255, 255, 0.2);
  width: 0% !important;
}

.bento-monthly {
  grid-column: 1 / -1;
  min-height: 240rpx;
}

.bento-big-num {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 72rpx;
  font-weight: 900;
  color: var(--primary-text);
  line-height: 1;
  margin: 8rpx 0 4rpx;
}

.bento-hint {
  font-size: 18rpx;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: rgba(204, 242, 0, 0.6);
}

.bento-bg-icon {
  position: absolute;
  right: -20rpx;
  bottom: -20rpx;
  font-size: 140rpx;
  opacity: 0.05;
}

.text-primary { color: var(--primary-text); }

/* Date Picker Button */
.date-picker-btn {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.10);
  border-radius: 12rpx;
  padding: 8rpx 20rpx;
  font-size: 22rpx;
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.05em;
}

/* ====== Custom Date Picker Modal ====== */
.picker-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(8rpx);
  z-index: 999;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.2s ease;
}

.picker-overlay.show {
  opacity: 1;
  pointer-events: all;
}

.picker-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.95);
  width: 580rpx;
  background: rgba(28, 28, 28, 0.98);
  border: 1px solid rgba(255, 255, 255, 0.10);
  border-radius: 32rpx;
  padding: 32rpx;
  z-index: 1000;
  box-shadow: 0 24rpx 80rpx rgba(0, 0, 0, 0.6);
  transition: transform 0.2s ease, opacity 0.2s ease;
}

.picker-overlay.show .picker-modal {
  transform: translate(-50%, -50%) scale(1);
  opacity: 1;
}

.picker-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.picker-nav {
  width: 64rpx;
  height: 64rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.picker-nav-arrow {
  font-size: 36rpx;
  color: #fff;
  font-weight: 300;
}

.picker-month-label {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 28rpx;
  font-weight: 800;
  color: #fff;
  letter-spacing: 0.05em;
}

.picker-day-headers {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  margin-bottom: 8rpx;
}

.picker-day-h {
  text-align: center;
  font-size: 20rpx;
  font-weight: 800;
  color: rgba(255, 255, 255, 0.30);
  padding: 8rpx 0;
}

.picker-days-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 4rpx;
}

.picker-day-cell {
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 16rpx;
  color: rgba(255, 255, 255, 0.50);
  transition: background 0.15s;
}

.picker-day-cell.empty {
  background: transparent;
}

.picker-day-cell:not(.empty):active {
  background: rgba(255, 255, 255, 0.08);
}

.picker-day-cell.today {
  color: #fff;
  background: rgba(255, 255, 255, 0.08);
}

.picker-day-cell.selected {
  background: var(--primary);
  color: #000;
  font-weight: 800;
}

.picker-today-btn {
  margin-top: 20rpx;
  width: 100%;
  padding: 16rpx 0;
  background: rgba(204, 242, 0, 0.10);
  border-radius: 16rpx;
  font-size: 22rpx;
  font-weight: 800;
  color: var(--primary-text);
  text-align: center;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

.picker-today-btn:active {
  background: rgba(204, 242, 0, 0.20);
}

/* ========================
   日间主题覆盖（day mode overrides）
   ======================== */
.page.theme-day {
  background: #f5f5f5;
}

.page.theme-day .today-title {
  color: #1a1a1a;
}

.page.theme-day .timer-card {
  background: rgba(0, 0, 0, 0.10);
  border: 1rpx solid rgba(0, 0, 0, 0.12);
}

.page.theme-day .timer-label {
  color: rgba(0, 0, 0, 0.45);
}

.page.theme-day .timer-display {
  color: #1a1a1a;
  text-shadow: none;
}

.page.theme-day .stat-val {
  color: #1a1a1a;
}

.page.theme-day .stat-key {
  color: rgba(0, 0, 0, 0.40);
}

.page.theme-day .stat-divider {
  background: rgba(0, 0, 0, 0.10);
}

.page.theme-day .section-title {
  color: #1a1a1a;
}

.page.theme-day .section-badge {
  color: #a1a1aa;
}

.page.theme-day .section-badge.active {
  color: var(--primary-text);
}

.page.theme-day .section-link {
  color: #1a1a1a;
}

.page.theme-day .exercise-card {
  background: rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.08);
}

.page.theme-day .ec-header {
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.05);
}

.page.theme-day .ec-img-wrap {
  background: rgba(0, 0, 0, 0.04);
}

.page.theme-day .ec-name {
  color: #1a1a1a;
}

.page.theme-day .ec-sets-count {
  color: #71717a;
}

.page.theme-day .ec-vol-num {
  color: var(--primary-text);
}

.page.theme-day .ec-vol-label {
  color: #71717a;
}

.page.theme-day .ec-set-item {
  background: rgba(0, 0, 0, 0.04);
}

.page.theme-day .ec-set-val {
  color: #1a1a1a;
}

.page.theme-day .ec-set-unit {
  color: #1a1a1a;
}

.page.theme-day .ec-set-sep {
  color: #1a1a1a;
}

.page.theme-day .empty-card {
  background: rgba(0, 0, 0, 0.03);
  border: 1px dashed rgba(0, 0, 0, 0.10);
}

.page.theme-day .empty-hint {
  color: #a1a1aa;
}

.page.theme-day .recent-item {
  background: rgba(0, 0, 0, 0.03);
}

.page.theme-day .recent-date {
  color: #1a1a1a;
}

.page.theme-day .recent-stat {
  color: #71717a;
}

.page.theme-day .history-session-item {
  background: rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.08);
}

.page.theme-day .history-session-time {
  color: #3ab24e;
}

.page.theme-day .history-session-stat {
  color: #71717a;
}

.page.theme-day .bento-card {
  background: rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.08);
}

.page.theme-day .bento-label {
  color: rgba(0, 0, 0, 0.35);
}

.page.theme-day .bento-day {
  color: #1a1a1a;
}

.page.theme-day .bento-month {
  color: #71717a;
}

.page.theme-day .bento-month.text-primary {
  color: var(--primary-text);
}

.page.theme-day .bento-month.text-muted {
  color: rgba(0, 0, 0, 0.25);
}

.page.theme-day .bento-progress-bar {
  background: rgba(0, 0, 0, 0.08);
}

.page.theme-day .bento-progress-fill {
  background: var(--primary);
}

.page.theme-day .bento-progress-fill.muted {
  background: rgba(0, 0, 0, 0.08);
}

.page.theme-day .bento-big-num {
  color: var(--primary-text);
}

.page.theme-day .bento-hint {
  color: rgba(0, 0, 0, 0.40);
}

.page.theme-day .date-picker-btn {
  background: rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.10);
  color: #1a1a1a;
}

.page.theme-day .picker-overlay {
  background: rgba(0, 0, 0, 0.4);
}

.page.theme-day .picker-modal {
  background: rgba(245, 245, 245, 0.98);
  border: 1rpx solid rgba(0, 0, 0, 0.10);
}

.page.theme-day .picker-nav {
  background: rgba(0, 0, 0, 0.05);
}

.page.theme-day .picker-nav-arrow {
  color: #1a1a1a;
}

.page.theme-day .picker-month-label {
  color: #1a1a1a;
}

.page.theme-day .picker-day-h {
  color: rgba(0, 0, 0, 0.30);
}

.page.theme-day .picker-day-cell {
  color: rgba(0, 0, 0, 0.50);
}

.page.theme-day .picker-day-cell.today {
  color: #1a1a1a;
  background: rgba(0, 0, 0, 0.06);
}

.page.theme-day .picker-day-cell.selected {
  background: var(--primary);
  color: #000;
}

.page.theme-day .picker-today-btn {
  background: rgba(204, 242, 0, 0.10);
  color: var(--primary-text);
}

.page.theme-day .picker-today-btn:active {
  background: rgba(204, 242, 0, 0.20);
}

/* ====== Calendar Mini ====== */
.calendar-card {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 24rpx;
  padding: 24rpx;
  margin-top: 24rpx;
}

.cal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.cal-nav {
  width: 56rpx;
  height: 56rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 14rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  color: #fff;
  font-weight: 300;
}

.cal-nav:active {
  background: rgba(255, 255, 255, 0.10);
}

.cal-month-label {
  font-family: 'Lexend Deca', sans-serif;
  font-size: 26rpx;
  font-weight: 800;
  color: #fff;
  letter-spacing: 0.05em;
}

.cal-weekdays {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  margin-bottom: 8rpx;
}

.cal-weekdays text {
  text-align: center;
  font-size: 18rpx;
  font-weight: 800;
  color: rgba(255, 255, 255, 0.30);
  padding: 6rpx 0;
}

.cal-days {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 4rpx;
}

.cal-days text {
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.50);
  border-radius: 16rpx;
}

.day-worked {
  background: transparent;
  color: inherit;
  font-weight: 600;
  border: 3rpx solid var(--primary);
}

.day-today {
  border: 2rpx solid rgba(255, 255, 255, 0.30);
}

.day-worked.day-today {
  box-shadow: 0 0 0 4rpx rgba(200, 230, 0, 0.5);
}

/* Day theme overrides */
.page.theme-day .calendar-card {
  background: rgba(0, 0, 0, 0.05);
  border: 1rpx solid rgba(0, 0, 0, 0.08);
}

.page.theme-day .cal-nav {
  background: rgba(0, 0, 0, 0.05);
}

.page.theme-day .cal-nav:active {
  background: rgba(0, 0, 0, 0.10);
}

.page.theme-day .cal-month-label {
  color: #1a1a1a;
}

.page.theme-day .cal-weekdays text {
  color: rgba(0, 0, 0, 0.30);
}

.page.theme-day .cal-days text {
  color: rgba(0, 0, 0, 0.50);
}

.page.theme-day .day-today {
  border: 2rpx solid rgba(0, 0, 0, 0.20);
}

.page.theme-day .day-worked {
  border-color: #9cb02c;
  background-color: var(--primary);
}

.page.theme-night .day-worked {
  border-color: var(--primary);
}
